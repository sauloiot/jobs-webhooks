{
  "name": "jobs-webhooks",
  "version": "0.1.0",
  "description": "Gateway de webhooks do Joblly — recebe webhooks de provedores externos e publica em filas SQS",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0",
    "dotenv": "^16.4.0",
    "express": "^5.0.0"
  }
}
